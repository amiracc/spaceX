import { ComponentStyleConfig } from "@chakra-ui/react";

const SearchBar: ComponentStyleConfig = {
  baseStyle: () => ({
    input: {
      color: "white",
      width: "30%",
      mb: 4,
    },
  }),
};

export default SearchBar;
